# ---------------------------------------------
# AHN Ransomware Simulation Tool - Requirements
# ---------------------------------------------
# Only core libraries used across the dashboards.
# Versions are pinned for reproducibility and
# compatibility with Streamlit + Plotly visualizations.

# Data handling & computation
pandas==2.2.2          # dataframes, CSV ingestion, transformation
numpy==1.26.4          # numerical operations & simulations

# Visualization libraries
plotly==5.22.0         # interactive graphs & heatmaps (px & go)
matplotlib==3.9.0      # fallback visualizations / static figures
altair==5.3.0          # lightweight declarative charts if needed

# Web application framework
streamlit==1.37.0      # interactive UI for the simulation tool
